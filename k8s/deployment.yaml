apiVersion: apps/v1
kind: Deployment
metadata:
  name: html-app
  namespace: argocdv1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: html-app
  template:
    metadata:
      labels:
        app: html-app
    spec:
      containers:
      - name: nginx
        image: rohanworld101/my-html-app:189419b3349e31b34334f0084455963327643701
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: html-service
spec:
  selector:
    app: html-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
